<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.jedit.kenklin">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.VerificationActivity"></activity>
        <activity
            android:name=".activities.SignUpActivity"
            android:label="@string/register" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/no_actionbar" />
        <activity
            android:name=".activities.ServiceRequestActivity"
            android:label="@string/service_req" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/no_actionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Dashboard" />
    </application>

</manifest>